{% extends "template.html.j2" %}
{% block content %}
    <aside>
        <h4>{{ dt }}</h4>
        {% if contents_html != "" %}
            <h5>Contents:</h5>
            <div id="contents">
                {{ contents_html|safe }}
            </div>
        {% endif %}
        <h5>This Category:</h5>
        <ul>
            <li><b><a href="{{'/articles#'+category.replace(' ', '_')}}">{{category}}</a></b></li>
        </ul>
        <h5>Related Articles:</h5>
        <ul>
            {% for id_, title, dt, category in related %}
                <li><a href="{{'/article?id=%i' % id_}}">{{title}}</a></li>
            {% endfor %}
        </ul>
        <h5>Other Categories:</h5>
        {% for category_name in othercategories %}
            <ul>
                <li><a href="{{'/articles#'+category_name.replace(' ', '_')}}">{{category_name}}</a></li>
            </ul>
        {% endfor %}
    </aside>
    {{ md_html|safe }}
{% endblock %}